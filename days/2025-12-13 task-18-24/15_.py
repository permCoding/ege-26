def f(x, A): 
    O1 = 25 <= x <= 64  # тут была ошибка - было 65
    O2 = 40 <= x <= 115  # тут была ошибка - было 116
    O3 = A[0]<= x <= A[1] 
    return O1 <= ((O2 and (not O3)) <= (not O1))


lst = [  # и тут везде были ошибки 
    (25, 40), (25, 64), (25, 115), 
    (40, 64), (40, 115), 
    (64, 115) 
]

m = 9**9
for A in lst: 
    if all(f(x, A) for x in range(10, 150)): 
        m = min(m, A[1]-A[0])
print(m)